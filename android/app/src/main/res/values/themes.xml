<resources>
    <!-- Compose base theme using platform Material theme (no AppCompat dependency needed) -->
    <!-- windowBackground matches app dark canvas to eliminate white flash on startup -->
    <style name="Theme.NextAI" parent="android:Theme.Material.Light.NoActionBar">
        <item name="android:windowBackground">#0D0D12</item>
        <item name="android:windowIsTranslucent">false</item>
        <item name="android:windowNoTitle">true</item>
        <item name="android:statusBarColor">#0D0D12</item>
        <item name="android:navigationBarColor">#0D0D12</item>
    </style>
</resources>
