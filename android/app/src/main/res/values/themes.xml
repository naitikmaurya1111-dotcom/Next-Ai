<resources>
    <!-- Base application theme. Uses Android built-in Material theme for Compose root window -->
    <style name="Theme.NextAI" parent="@android:style/Theme.Material.Light.NoActionBar">
        <item name="android:windowBackground">@android:color/white</item>
    </style>
</resources>
