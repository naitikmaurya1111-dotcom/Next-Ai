<resources>
    <!-- Compose dark night theme using platform Material theme -->
    <!-- windowBackground references @color/obsidian_background safely across all Android versions -->
    <style name="Theme.NextAI" parent="android:Theme.Material.NoActionBar">
        <item name="android:windowBackground">@color/obsidian_background</item>
        <item name="android:windowIsTranslucent">false</item>
        <item name="android:windowNoTitle">true</item>
        <item name="android:statusBarColor">@color/obsidian_background</item>
        <item name="android:navigationBarColor">@color/obsidian_background</item>
    </style>
</resources>
